services:
  prometheus:
    image: prom/prometheus:v2.44.0
    container_name: hellocava-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    extra_hosts:
      - "host.docker.internal:host-gateway"
